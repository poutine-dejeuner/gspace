[project]
name = "gspace"
version = "0.1.0"
description = "Geometric subspaces for algebraic computation in LLMs"
requires-python = ">=3.12"
dependencies = [
    "torch>=2.0",
    "transformers>=4.40",
    "accelerate",
    "datasets",
    "scikit-learn",
    "matplotlib",
    "numpy",
    "tqdm",
    "pandas",
    "peft>=0.20.0",
    "html2text>=2025.4.15",
    "huggingface-hub>=1.29.0",
    "bitsandbytes>=0.50.2",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
